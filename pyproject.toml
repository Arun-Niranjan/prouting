[project]
name = "prouting"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=0.2.14",
    "langchain>=0.3.13",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
